#include<stdio.h>

int main()
{
	int data[10],right[10];
	int i,n = 9,t,len;
	for(i = 0; i < n ;i++){
		data[i] = i+1;
		if(i != n - 1){
			right[i] = i + 1;
		}
	}
	len = n;
	len++;
	t = 0;
	while(t != 0){
		if(data[right[t] > 6]{
			rifht[len - 1] = right[t];
			rifht[t] = len - 1;
		}
		t = right[t];
	}
	
	
}